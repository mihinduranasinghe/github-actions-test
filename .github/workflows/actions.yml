name: Actions wordkflow

on: [push]

jobs:
  run-github-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Simmple JS actions
        id: greet
        uses: actions/hello-world-javascript-action@v1.1
        with:
          who-to-greet: Mihindu
      - name: Log greeting time
        run: echo "${{steps.greet.outputs.time}}"
